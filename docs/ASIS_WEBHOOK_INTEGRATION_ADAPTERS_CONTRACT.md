# Asis integration-adapter contract

Artifact type: `contract`

Provider request mechanics belong in adapters. Provider credentials never appear in adapter results.
Failures are normalized to stable public fields and
raw provider bodies are not returned to application services.

No deployment is performed by this public judging package.
